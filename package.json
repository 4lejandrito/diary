{
  "name": "diary",
  "description": "XXI Century Personal Diary",
  "version": "0.0.2",
  "private": true,
  "engines": {
    "node": "0.10"
  },
  "dependencies": {
    "eventemitter2": "^0.4.14",
    "events": "^1.0.2",
    "exproose": "*",
    "extend": "^2.0.0",
    "underscore": "^1.8.2",
    "watch": "^0.14.0"
  },
  "devDependencies": {
    "chai-as-promised": "^4.2.0",
    "sinon": "^1.12.2",
    "sinon-chai": "^2.7.0",
    "chai": "*",
    "istanbul": "*",
    "mocha": "*",
    "node-inspector": "*",
    "nodemon": "^1.3.6"
  },
  "scripts": {
    "start": "export NODE_PATH=. && node src/app",
    "watch": "export NODE_PATH=. && nodemon src/app.js",
    "debug": "export NODE_ENV=test && export NODE_PATH=. && node-debug src/app.js",
    "test": "export NODE_ENV=test && export NODE_PATH=. && mocha -R spec --recursive",
    "coverage": "export NODE_ENV=test && export NODE_PATH=. && ./node_modules/istanbul/lib/cli.js cover ./node_modules/mocha/bin/_mocha -- -R spec --recursive"
  }
}
